{
  "name": "agents-flows-recipes",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "poml/**/*.poml": "python scripts/check_poml_headers.py --"
  }
}
