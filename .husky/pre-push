#!/usr/bin/env sh

set -e

# Validate POML headers repo-wide before pushing
files="$(git ls-files 'poml/**/*.poml' | tr '\n' ' ' || true)"
if [ -n "$files" ]; then
  python scripts/check_poml_headers.py -- $files
else
  echo "No POML files found"
fi

exit 0
